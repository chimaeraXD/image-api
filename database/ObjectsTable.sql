CREATE TABLE Objects (
                        object_id SMALLINT NOT NULL AUTO_INCREMENT,
                        object_name varchar(255) NOT NULL,
                        CONSTRAINT Pk_Object PRIMARY KEY (object_id)
)